import React from 'react';
import ProjectListTable from './ProjectListTable';

const ProjectList: React.FC = () => {
  // For now, just render the ProjectListTable component
  return <ProjectListTable />;
};

export default ProjectList;
